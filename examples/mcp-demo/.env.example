# Agent wallet private key (must hold USDC + native token for gas)
# Use the relayer private key or any funded wallet
AGENT_PRIVATE_KEY=0x...

# Chain: flowEvm | base | baseSepolia
CHAIN=base

# Merchant address — receives subscription payments
MERCHANT_ADDRESS=0x...

# Service port (the demo price feed API)
SERVICE_PORT=4000
